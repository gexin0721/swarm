# 无人机技术路线规划 —— ESP32无人机系统设计

> **版本**: v0.5.0 | **更新日期**: 2026-02-02

## 核心目标
基于ESP32开发无人机集群控制系统，采用**MQTT通信**、**LQR控制**、**单AI架构**，构建可扩展的智能无人机平台。

### 技术选型总览

| 模块 | 方案 | 理由 |
|------|------|------|
| 通信协议 | MQTT | 发布/订阅模式，适合集群扩展 |
| 控制算法 | LQR | 多变量最优控制，适合四旋翼 |
| AI架构 | 单AI+6工具 | 轻量化，响应快 |


## 一、硬件选型与引脚分配

### 核心硬件清单
| 类别 | 型号/规格 | 引脚分配 | 备注 |
|------|-----------|----------|------|
| **主控** | ESP32-S3-DevKitC-1 | - | 双核240MHz，45个GPIO |
| **姿态** | MPU6050 | SDA=GPIO8, SCL=GPIO9, INT=GPIO6 | I2C 400kHz，中断驱动100Hz控制循环 |
| **定位** | NEO-M7 GPS | RX=GPIO17, TX=GPIO18 | UART1 9600bps，DMA接收 |
| **动力** | 8520空心杯电机×4 + 电机驱动×4 | M1: GPIO1/2+PWM(GPIO42), M2: GPIO3/4+PWM(GPIO41), M3: GPIO40/39+PWM(GPIO38), M4: GPIO37/36+PWM(GPIO35) | 每电机2个GPIO+1个PWM通道, LEDC Ch0-3, 20kHz |
| **显示** | 0.96" OLED SSD1306 | SDA=GPIO21, SCL=GPIO22 | I2C 400kHz (独立总线) |
| **电源** | 3.7V 1000mAh锂电 | ADC=GPIO5 | 电压监测 |
| **指示** | LED(红/绿) + 蜂鸣器 | GPIO10/11/12 | 状态指示/报警 |
| **调试** | USB OTG | 原生USB | PID参数实时调整 |
| **扩展** | UART2预留 | RX=GPIO15, TX=GPIO16 | 遥控器/数传模块 |

### 初号机配置（JY60方案）
| 差异模块 | 型号 | 引脚分配 | 备注 |
|----------|------|----------|------|
| 姿态传感器 | JY60 | RX=GPIO15, TX=GPIO16 | UART2 9600bps |
| GPS模块 | NEO-M8N | RX=GPIO17, TX=GPIO18 | UART1 9600bps |

---

## 二、系统架构设计

### 主控制循环（100Hz LQR控制）
```
GPIO6中断触发 (10ms周期):
├── 读取MPU6050/JY60数据 (~1ms)
├── 卡尔曼滤波状态估计 (~0.5ms)
├── LQR控制计算 u = -Kx (~0.5ms)
├── 更新电机PWM (~0.2ms)
├── MQTT数据发布 (~1ms)
└── 总耗时 <4ms (留6ms余量)
```

### LQR控制算法设计

**状态空间模型：**
```
状态向量 x = [φ, θ, ψ, φ̇, θ̇, ψ̇, z, ż]ᵀ
         (roll, pitch, yaw, 角速度, 高度, 垂直速度)

控制输入 u = [u1, u2, u3, u4]ᵀ (四个电机推力)

状态方程: ẋ = Ax + Bu
控制律:   u = -Kx (K为LQR增益矩阵，离线计算)
```

**ESP32实现：**
- K矩阵离线计算（MATLAB/Python）
- ESP32只做矩阵乘法 u = -Kx
- 100Hz控制频率，计算耗时<1ms

### DMA资源优化配置
| DMA通道 | 用途 | 作用 |
|---------|------|------|
| Ch0 | UART1-RX (GPS) | 减少CPU中断，提高数据完整性 |
| Ch1 | ADC (电池电压) | 连续采样，提高精度 |
| Ch2 | WiFi-TX (UDP) | 异步发送，避免阻塞控制循环 |
| Ch3 | I2C-RX (MPU6050) | 加速数据读取 |


## 三、三层核心逻辑（无人机端→通信→上位机ROS）
### 1. 无人机端（ESP32固件，C/Arduino实现）
#### 核心功能：
- **多串口数据处理**：
  - UART0（PID调试）：接收电脑发来的PID参数指令（如"P1=2.5,P2=0.3"），实时更新PID系数；
  - UART1（GPS）：解析NEO-M7的NMEA协议（取`$GNRMC`语句），提取经纬度、卫星数；
  - UART2（备用）：预留扩展接口（原JY60改为MPU6050 IIC接口）。
- **边缘计算优化**：
  - **PID控制**：姿态PID控制在ESP32本地完成，确保实时性
  - **坐标转换**：GPS经纬度→UTM坐标转换在ESP32完成，减少通信负载
- **数据打包发送**：将"姿态+UTM坐标+电池电压+PID参数"整合成JSON，通过WiFi-UDP发往上位机（50Hz频率），示例：
  ```json
  {
    "drone_id": "test_01",
    "roll": 1.1, "pitch": 0.8, "yaw": 359.2,  // JY60数据
    "utm_x": 1234.56, "utm_y": 5678.90,      // 已转换UTM坐标
    "sat": 13, "batt": 3.85,                  // 卫星数、电池电压
    "pid_p": 2.5, "pid_i": 0.3, "pid_d": 0.8  // 当前PID参数
  }
  ```
- **控制指令执行**：接收上位机的"期望姿态指令"（roll/pitch/throttle），通过PID计算输出4路PWM，驱动电机。


### 2. 通信层（MQTT）

#### MQTT架构
```
                    ┌─────────────────────┐
                    │   MQTT Broker       │
                    │   (Mosquitto:1883)  │
                    └──────────┬──────────┘
           ┌───────────────────┼───────────────────┐
           ↓                   ↓                   ↓
      ESP32 #1            ESP32 #2            ROS上位机
```

#### Topic设计
| Topic | 方向 | QoS | 说明 |
|-------|------|-----|------|
| `swarm/{id}/data` | 无人机→上位机 | 0 | 实时数据（50Hz） |
| `swarm/{id}/cmd` | 上位机→无人机 | 1 | 单机控制指令 |
| `swarm/all/cmd` | 上位机→所有 | 1 | 集群广播指令 |
| `swarm/{id}/status` | 无人机→上位机 | 1 | 状态变更通知 |

#### 消息格式
**数据上报 (swarm/{id}/data)**
```json
{
  "ts": 1706860800000,
  "roll": 1.1, "pitch": 0.8, "yaw": 359.2,
  "lat": 30.123, "lon": 120.654,
  "sat": 13, "batt": 3.85
}
```

**控制指令 (swarm/{id}/cmd)**
```json
{
  "type": "move",
  "target": {"lat": 30.124, "lon": 120.655, "alt": 10}
}
```


### 3. 上位机ROS功能包（仅笔记本运行，7个包协同工作）

#### 🚁 **无人机核心功能包**（4个包）
| ROS功能包          | 核心作用                          | 关键话题/指令                          |
|---------------------|-----------------------------------|----------------------------------------|
| **udp_ros_bridge**  | 通信桥梁：UDP↔ROS话题转换         | 接收：无人机JSON数据→发布`/test_01/imu`（姿态）、`/test_01/gps`（GPS）；<br>发送：订阅`/test_01/cmd_vel`（控制指令）→ 转UDP给ESP32 |
| **swarm_planner**   | 路径规划：语音指挥+轨迹生成      | 1. 订阅`/cmd/voice`语音指令→生成路径（发布`/test_01/path`）；<br>2. 简单路径规划算法（A*基础版本） |
| **gazebo_simulator** | 仿真验证：算法测试+安全预演      | 1. 仿真多机协同算法验证；<br>2. 路径规划算法测试；<br>3. 危险场景安全测试 |


#### 🎤 **智能语音AI功能包**（简化架构）
| ROS功能包          | 核心作用                          | 实现状态 |
|---------------------|-----------------------------------|----------|
| **voice**           | 语音识别：FunASR中文识别          | ✅ 已实现 |
| **ai_assistant**    | 单AI对话：指令理解+MCP工具调用    | 🔄 待简化 |

#### 🤖 **AI模块简化设计**

**单AI架构**（替代原双AI）：
```
用户语音 → 语音识别 → 单AI理解 → MCP工具调用 → 无人机执行
                           ↓
                      语音反馈
```

**精简后的MCP工具（6个）**：
| 工具 | 功能 |
|------|------|
| drone_move | 控制无人机移动 |
| drone_hover | 悬停 |
| drone_land | 降落 |
| drone_return | 返航 |
| drone_status | 查询状态 |
| swarm_broadcast | 集群广播指令 |

**指令解析示例**：
```
"飞到那边" → drone_move(target)
"所有无人机返航" → swarm_broadcast("return_home")
"电池怎么样" → drone_status() → 语音播报
```


## 四、验证步骤（从单模块到智能系统联调）

### 🔧 **Step 0：语音AI环境一键配置**（必需的前置步骤）
**重要：在进行任何语音AI相关测试前，必须先运行环境配置脚本**

```bash
cd Software/Hive_control_ROS/环境
sudo chmod +x robot.sh
sudo ./robot.sh
```

**该脚本自动完成的关键配置：**
1. **系统依赖安装**：
   - 音频库：`libasound2-dev`, `sox`, `portaudio19-dev`
   - 开发工具：`vim`, `terminator`, `cmake`, `git`
   - Python包：`websocket-client`, `vosk`, `sounddevice`

2. **Vosk语音模型管理**：
   - 自动下载：`vosk-model-cn-kaldi-multicn-0.15`（约200MB）
   - 智能解压：处理嵌套目录，修复解压问题
   - 模型验证：测试加载和识别功能

3. **设备权限配置**：
   - CH9102串口设备权限（`ch9102_udev.sh`）
   - 音频设备访问权限
   - 动态链接库配置

4. **库文件安装**：
   - 根据系统架构自动选择（x64/arm64/x86）
   - cJSON库编译安装
   - 讯飞TTS引擎依赖

5. **功能测试验证**：
   - 自动创建`test_vosk.py`测试脚本
   - 验证语音识别模型完整性
   - 测试音频处理流程

**配置完成后建议重启系统，然后继续后续步骤。**

###  Step 1：硬件单独测试（确保每个模块能用）
1. **串口测试**：用电脑USB接ESP32的UART0（PID调试口），通过串口助手发送指令，确认ESP32能接收并回复（验证调试串口正常）；
2. **GPS测试**：室外放置M8N，用串口助手读UART1数据，确认卫星数≥6，经纬度稳定（误差<2米）；
3. **姿态测试**：接JY60到UART2，晃动无人机，确认姿态数据连续变化（无跳变）；
4. **电机测试**：给电调供电，通过ESP32输出PWM，确认4个电机转速可调（0-100%），对角转向相反。

###  Step 2：固件调试（无人机端核心逻辑）
1. 先实现“单模块+OLED显示”：比如JY60姿态在OLED实时刷新，GPS卫星数显示；
2. 加入WiFi通信：确保上位机能收到完整JSON数据（用网络调试助手验证）；
3. 调试PID串口：通过UART0发送“PID_P=2.5”，确认OLED能显示新参数（实时生效）。

###  Step 3：ROS联调（上位机控制逻辑）
1. 启动`udp_ros_bridge`：在RViz中添加`/test_01/gps`话题，看到红色圆点跟随无人机移动；
2. 启动`drone_planner`：输入目标UTM坐标（比如当前坐标+3米），看到`/test_01/path`显示直线路径；
3. 地面测试：无人机放在地面，发送控制指令，确认电机能根据GPS偏差调整转速（比如偏向目标方向时，对应电机加速）。

###  Step 4：试飞验证（低空小范围测试）
1. 室外无遮挡环境，低空悬停（1米高），观察姿态是否稳定（PID参数不合适则微调UART0的参数）；
2. 简单路径跟踪：控制无人机从A点飞到3米外B点，确认能大致按规划路径飞行，GPS定位准确。


## 五、后续优化方向
1. **硬件优化**：系统验证稳定后，可考虑使用MPU6050替代JY60，NEO-6M替代NEO-M8N，进一步降低系统成本；
2. **多机扩展**：新增无人机时，只需修改`drone_id`和IP，ROS功能包无需重构（复用`udp_ros_bridge`和`drone_planner`）；
3. **功能升级**：UART3可接光流模块（室内定位），解决GPS室内失效问题。


## 核心总结

### 🚀 **系统架构升级（v0.5.0）**
整个方案基于ESP32构建智能无人机集群控制系统，采用三大核心技术：

| 模块 | 技术方案 | 优势 |
|------|----------|------|
| 通信 | MQTT | 发布/订阅，集群扩展简单 |
| 控制 | LQR | 多变量最优，稳定性好 |
| AI | 单AI+6工具 | 轻量快速，响应及时 |

### 🔄 **数据流闭环**
```
用户语音 → FunASR识别 → 单AI理解 → MCP工具 → MQTT发布 → 无人机执行
                                                    ↓
                                              状态上报 → 语音播报
```

### 🎯 **集群控制优势**
- **单机控制**：`swarm/{id}/cmd` 精确控制
- **集群广播**：`swarm/all/cmd` 一句话控制所有无人机
- **动态扩展**：新增无人机只需订阅Topic，无需改代码